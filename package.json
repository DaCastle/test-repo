{
  "name": "new-tester",
  "version": "0.1.8",
  "private": true,
  "dependencies": {
    "@sentry/browser": "^5.8.0",
    "axios": "^0.19.0",
    "firebase": "^7.3.0",
    "prettier-standard": "^15.0.1",
    "react": "^16.11.0",
    "react-dom": "^16.11.0",
    "react-firebase-hooks": "^2.1.1",
    "react-lazyload": "^2.6.5",
    "react-scripts": "3.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.3.2",
    "husky": "^3.0.9"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm --no-git-tag-version version patch && git add * && prettier-standard --staged"
    }
  }
}
